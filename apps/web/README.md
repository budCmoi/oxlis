# OXLIS Web

Next.js frontend for the OXLIS marketplace.

## Local Development

Run the frontend from this directory with:

```bash
npm run dev
```

The app expects the API to be available through `NEXT_PUBLIC_API_URL`. For a full local setup, environment variables, and deployment instructions, use the root repository README.
